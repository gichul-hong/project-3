Diffusers: Classifier-Free Guidance
image = pipeline(
prompt="a photo of a cat holding a sign that says hello wo
negative_prompt="bad quality, low-resolution, distorted"
num_inference_steps=28,
Update
height=512,
width=512,
guidance_scale=7.0,
Make the images far from
generator=torch.Generator() .manual_seed(0)
the negative prompts
).images [0]
(instead of unconditional generation)
hello
hello
world
world
25